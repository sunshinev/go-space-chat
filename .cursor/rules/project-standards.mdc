---
description: 本项目的通用开发规范，始终生效。每次修复问题时，必须把解决方案沉淀到本规则文件中。
alwaysApply: true
---

# 本项目开发规范（强制）

## 总则

- **目标**：保证代码质量、可维护性和知识沉淀，让“踩过的坑只踩一次”。
- **适用范围**：本仓库中所有代码变更（后端 Go、前端、配置、脚本等）。
- **强制要求**：每当在改动代码时发现一个明确的问题并完成修复，必须在本文件中新增一条对应的「问题 & 解决方案」记录。

## 使用方式

- **当你在写 / 改代码时**：
  1. 发现了一个问题（bug、设计缺陷、性能问题、可读性问题、并发安全问题等）。
  2. 分析并实现了解决方案。
  3. 在提交代码前，把这个问题和解决方案以「问题条目」的形式追加到本文档对应的小节里。
  4. 优先抽象为可以复用的“原则”或“模式”，而不仅仅是某一行代码的 patch 描述。
  5. 在认为代码已修改完成、准备提交之前，**必须依次触发并执行以下两个 Skill，对本次改动进行检查**：
     - `optimize-go-code`：Go 代码性能 / 分配 / 并发优化检查。
     - `go-code-review`：Go 代码 CR（正确性、并发安全、风格等）检查。
     （如果本次改动是非 Go 代码，可以只执行第二个或改用更合适的 CR Skill，但默认优先考虑这两项。）
- **当 AI 在本项目中参与开发时**：
  - 遵守本规则描述的习惯和经验。
  - 在帮助修复问题时，如发现是一个通用模式，也应建议用户把经验写入这里。

## 记录格式规范

每个问题建议使用下面的模板追加到合适的小节（可以根据需要新建小节）：

- **问题简述**：一句话描述问题现象或根因。
- **影响范围**：说明影响的是哪一类代码或模块（例如：WebSocket 广播、登录统计、前端渲染等）。
- **错误示例（可选）**：用简短代码片段说明错误写法。
- **正确做法 / 解决方案**：给出推荐的写法或处理流程，尽量抽象成通用规则。
- **备注（可选）**：链接到相关 PR / 提交 ID 或外部资料。

> 示例：
> - **问题简述**：在 WebSocket 广播中并发写同一个连接导致数据竞争。
> - **影响范围**：所有通过 `*websocket.Conn` 进行写操作的广播逻辑。
> - **正确做法 / 解决方案**：同一条连接的写操作必须串行化（例如使用互斥锁或专门的写 goroutine），绝不能多个 goroutine 同时对同一 `conn` 调用 `WriteMessage`。

## 建议分类（可按需增删）

- **Go 代码 & 并发模式**
  - 记录典型的 goroutine 使用方式、channel 模式、锁的使用规范，以及与 gorilla/websocket、protobuf 等库相关的常见坑和最佳实践。
- **性能优化经验**
  - 记录通过 profiling / benchmark 得到的通用优化经验（例如：避免在热路径上频繁分配、如何复用 protobuf 消息等）。
- **接口设计与数据结构**
  - 记录 HTTP / WebSocket 接口的约定、响应格式规范，以及在实践中被证明更易维护的结构设计。
- **错误处理与日志规范**
  - 记录哪些错误必须向上抛出，哪些可以吞掉并打印日志，以及统一的日志格式和敏感信息处理要求。
- **前端交互与用户体验（如适用）**
  - 记录前端交互上的统一约定，比如提示文案风格、错误展示方式等。
- **前端构建与依赖管理（如适用）**
  - **问题简述**：`yarn install` / `yarn build` 失败，常见原因是 `yarn.lock` 将下载地址锁定到已废弃/证书异常的镜像域名，或在 production 模式下未安装 devDependencies（导致 `vue-cli-service` 缺失），以及旧依赖对新 Node 版本存在 engine 限制。
  - **影响范围**：`web_resource/` 前端依赖安装与打包流程（Vue CLI 4 / Yarn v1 项目尤甚）。
  - **正确做法 / 解决方案**：
    - 避免提交把 `resolved` 锁死到过期镜像（如 `registry.npm.taobao.org`）的 `yarn.lock`；必要时删除并重新安装生成新的 lock。
    - 用项目级 `.npmrc` 固定可用 registry（例如 `https://registry.npmmirror.com/` 或 `https://registry.npmjs.org/`），避免依赖开发者全局配置。
    - 打包需要 devDependencies 时，确保安装时显式开启：`yarn install --production=false`。
    - 若遇到依赖 engine 与本机 Node 不兼容：优先使用项目约定的 Node LTS（建议补充 `.nvmrc`）；临时方案可用 `yarn install --ignore-engines`，但应评估构建/运行风险。
- **部署与运维相关约定**
  - 记录与配置文件、环境变量、监控指标等相关的经验和踩坑记录。

## 执行要求

- **提交前自查**：
  - 在提交包含“问题修复”的改动前，先检查本文件是否已经记录了对应经验。
  - 如果没有，则补充一条新的「问题 & 解决方案」记录。
- **Review 时检查**：
  - 代码 Review（CR）时，除了看代码本身，也要检查：是否应该把这次发现的问题沉淀到本规则中。
- **AI 协作时的要求**：
  - 当 AI 帮助发现并修复了通用性问题时，应在回复中提醒开发者：把经验补充进本文件。

